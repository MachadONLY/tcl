node_modules/
dist/
.env.local
.env.*.local
.vscode/*.log
*.log
.DS_Store
Thumbs.db

# The current Premier League roster, ratings, manifests and player portraits
# are intentionally versioned so a GitHub ZIP reproduces the same game build.
